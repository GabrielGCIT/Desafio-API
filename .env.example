# Copie este arquivo para .env e preencha com seus valores reais.
# O arquivo .env nunca deve ir para o repositório (já está no .gitignore).

PORT=3000
JWT_SECRET=troque-por-uma-chave-forte-e-aleatoria
JWT_EXPIRATION=1h
